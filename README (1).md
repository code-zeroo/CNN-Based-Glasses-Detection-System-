Put two sample photos here for the "Try an example" buttons in the Gradio UI:

- `glasses_example.jpg`
- `noglasses_example.jpg`

They're referenced by `app.py` but not included in this repo (use your own
photos, or a couple of images from your training set that you have the
rights to share).
